# Screenshot Checklist

Add final screenshots here before publishing the GitHub repository.

Recommended files:

- `cpp-editor.png`: `examples/cpp-theme-color-specimen.cpp` open in the editor. Added.
- `project-tree.png`: CLion project window using Real Black.
- `settings-ui.png`: Settings or Plugins screen showing the Real Black UI shell.

Recommended capture setup:

- Use the latest packaged version of Real Black.
- Use a representative C++ project.
- Show the Project tool window and at least one modified/new/excluded file if possible.
- Avoid personal paths or private project names if publishing publicly.
- Keep images reasonably compressed for GitHub.

After adding screenshots, update the Screenshots section in `README.md` with image links.
